<p style="color: green"><%= notice %></p>

<h1>Comments</h1>

<div id="comments">
  <% @comments.each do |comment| %>
    <% #  if comment.date == @response["date"] %>
      <%= render comment %>
    <% if comment.user == current_user %>
      <%= link_to "Edit", edit_comment_path(comment.id) %>
    <% end %>
  <%# end %>
<% end %>
</div>

<%= link_to "New comment", new_comment_path %>
